import React from 'react';

const DifferentContent: React.FC = () => {
    return (
        <div className="tab-content">
            <h4>I'm a DIFFERENT tab, so I reset state 💣💥</h4>
        </div>
    );
};

export default DifferentContent;